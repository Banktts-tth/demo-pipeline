---
# Source: demo-pipeline/templates/serviceaccount.yaml
apiVersion: v1
kind: ServiceAccount
metadata:
  name: demopipeline-demo-pipeline
  labels:
    helm.sh/chart: demo-pipeline-0.1.0
    app.kubernetes.io/name: demo-pipeline
    app.kubernetes.io/instance: demopipeline
    app.kubernetes.io/version: "latest"
    app.kubernetes.io/managed-by: Helm
---
# Source: demo-pipeline/templates/configmap.yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: demopipeline-config
data:
  application.properties: |
    app.name=DemoApp
    app.version=1.0.0
---
# Source: demo-pipeline/templates/configserver.yaml
apiVersion: v1
kind: ConfigMap
metadata:
  name: demopipeline-configserver-config
  labels:
    app: demopipeline-configserver
data:
  application.yml: |
        
  application-dev.yml: |
---
# Source: demo-pipeline/templates/configserver.yaml
apiVersion: v1
kind: Service
metadata:
  name: demopipeline-configserver
  labels:
    app: demopipeline-configserver
spec:
  type: ClusterIP
  ports:
    - port: 8888
      targetPort: 8888
      protocol: TCP
      name: http
  selector:
    app: demopipeline-configserver
---
# Source: demo-pipeline/templates/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: demopipeline-demo-pipeline
  labels:
    helm.sh/chart: demo-pipeline-0.1.0
    app.kubernetes.io/name: demo-pipeline
    app.kubernetes.io/instance: demopipeline
    app.kubernetes.io/version: "latest"
    app.kubernetes.io/managed-by: Helm
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: demo-pipeline
    app.kubernetes.io/instance: demopipeline
---
# Source: demo-pipeline/templates/configserver.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: demopipeline-configserver
  labels:
    app: demopipeline-configserver
spec:
  replicas: 1
  selector:
    matchLabels:
      app: demopipeline-configserver
  template:
    metadata:
      labels:
        app: demopipeline-configserver
    spec:
      containers:
        - name: configserver
          image: "hyness/spring-cloud-config-server:latest"
          imagePullPolicy: IfNotPresent
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "dev"
          ports:
            - containerPort: 8888
          volumeMounts:
            - name: config-volume
              mountPath: /config
              readOnly: true
      volumes:
        - name: config-volume
          configMap:
            name: demopipeline-configserver-config
---
# Source: demo-pipeline/templates/deployment.yaml
apiVersion: apps/v1
kind: Deployment
metadata:
  name: demopipeline-demo-pipeline
  labels:
    helm.sh/chart: demo-pipeline-0.1.0
    app.kubernetes.io/name: demo-pipeline
    app.kubernetes.io/instance: demopipeline
    app.kubernetes.io/version: "latest"
    app.kubernetes.io/managed-by: Helm
spec:
  replicas: 5
  selector:
    matchLabels:
      app.kubernetes.io/name: demo-pipeline
      app.kubernetes.io/instance: demopipeline
  template:
    metadata:
      labels:
        app.kubernetes.io/name: demo-pipeline
        app.kubernetes.io/instance: demopipeline
    spec:
      serviceAccountName: demopipeline-demo-pipeline
      securityContext:
        {}
      containers:
        - name: demo-pipeline
          securityContext:
            {}
          image: "docker.io/bankttstth/demo-spring:latest"
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          volumeMounts:
            - name: my-config
              mountPath: /config
          livenessProbe:
            httpGet:
              path: /api/hello
              port: http
            initialDelaySeconds: 60
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /api/hello
              port: http
            initialDelaySeconds: 60
            periodSeconds: 10
          resources:
            {}
      volumes:
        - name: my-config
          configMap:
            name: demopipeline-config
---
# Source: demo-pipeline/templates/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: demopipeline-demo-pipeline
  labels:
    helm.sh/chart: demo-pipeline-0.1.0
    app.kubernetes.io/name: demo-pipeline
    app.kubernetes.io/instance: demopipeline
    app.kubernetes.io/version: "latest"
    app.kubernetes.io/managed-by: Helm
spec:
  rules:
    - host: "localhost"
      http:
        paths:
          - path: /
            pathType: ImplementationSpecific
            backend:
              service:
                name: demopipeline-demo-pipeline
                port:
                  number: 8080
---
# Source: demo-pipeline/templates/tests/test-connection.yaml
apiVersion: v1
kind: Pod
metadata:
  name: "demopipeline-demo-pipeline-test-connection"
  labels:
    helm.sh/chart: demo-pipeline-0.1.0
    app.kubernetes.io/name: demo-pipeline
    app.kubernetes.io/instance: demopipeline
    app.kubernetes.io/version: "latest"
    app.kubernetes.io/managed-by: Helm
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['sh', '-c']
      args: ['sleep 120; wget -qO- --timeout=5 --tries=1 http://demopipeline-demo-pipeline:8080/api/hello || exit 1']
  restartPolicy: Never
